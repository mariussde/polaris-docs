---
title: "Usando React Server Components y Server Actions en Next.js"
description: "Explora cómo aprovechar React Server Components y Server Actions en Next.js para construir aplicaciones web modernas y eficientes. Aprende cómo estas características mejoran el rendimiento y simplifican la lógica del servidor."
date: 05-09-2024
authors:
  - handle: mariussde
    username: Marius SDE
    handleUrl: "https://github.com/mariussde"
  - handle: vercel
    username: React Dev
    handleUrl: "https://github.com/vercel"
cover: "/react-next.png"
---

## Introducción: Mejorando Next.js con React Server Components

Next.js ha evolucionado para incluir características poderosas como React Server Components y Server Actions, que ofrecen una nueva forma de manejar el renderizado del lado del servidor y la lógica. Estas características proporcionan un enfoque más eficiente y optimizado para construir aplicaciones web, permitiéndote obtener datos y renderizar componentes en el servidor sin comprometer el rendimiento.

En esta entrada del blog, exploraremos cómo usar React Server Components y Server Actions en Next.js con ejemplos prácticos y fragmentos de código.

## ¿Qué son React Server Components?

React Server Components (RSC) son un nuevo tipo de componente introducido por React que te permite renderizar componentes en el servidor. Este enfoque ayuda a reducir la cantidad de JavaScript enviado al cliente y mejora el rendimiento al trasladar el trabajo de renderizado al servidor.

### Beneficios de React Server Components

- **Rendimiento Mejorado**: Al renderizar en el servidor, reduces la cantidad de JavaScript del lado del cliente y mejoras los tiempos de carga.
- **Experiencia de Usuario Mejorada**: Cargas iniciales de página más rápidas e interacciones más fluidas.
- **Obtención de Datos Simplificada**: Obtén datos en el servidor y pásalos directamente a los componentes.

### Ejemplo: Creando un Server Component

Aquí tienes un ejemplo básico de un React Server Component en una aplicación Next.js:

```jsx
// app/components/UserProfile.server.js
import { getUserData } from "../lib/api";

export default async function UserProfile() {
  const user = await getUserData();

  return (
    <div>
      <h1>{user.name}</h1>
      <p>{user.email}</p>
    </div>
  );
}
```

En este ejemplo, `UserProfile` es un server component que obtiene datos del usuario en el servidor y los renderiza.

## ¿Qué son Server Actions?

Server Actions son funciones que se ejecutan en el servidor en respuesta a interacciones del usuario u otros eventos. Te permiten manejar lógica del lado del servidor, como envíos de formularios o solicitudes API, directamente desde tus componentes React.

### Beneficios de Server Actions

- **Lógica del Servidor Simplificada**: Escribe código del lado del servidor directamente en tus componentes.
- **Seguridad Mejorada**: Maneja operaciones sensibles en el servidor en lugar del cliente.
- **Rendimiento Mejorado**: Reduce JavaScript del lado del cliente y traslada tareas al servidor.

### Ejemplo: Usando Server Actions

Aquí tienes cómo puedes usar Server Actions en una aplicación Next.js para manejar envíos de formularios:

```jsx
// app/actions/submitForm.js
import { saveFormData } from "../lib/api";

export async function submitForm(data) {
  await saveFormData(data);
  return { success: true };
}
```

```jsx
// app/components/ContactForm.js
"use client";

import { submitForm } from "../actions/submitForm";

export default function ContactForm() {
  const handleSubmit = async (event) => {
    event.preventDefault();
    const formData = new FormData(event.target);
    const result = await submitForm(Object.fromEntries(formData));
    if (result.success) {
      alert("¡Formulario enviado exitosamente!");
    }
  };

  return (
    <form onSubmit={handleSubmit}>
      <label>
        Nombre:
        <input type="text" name="name" required />
      </label>
      <label>
        Email:
        <input type="email" name="email" required />
      </label>
      <button type="submit">Enviar</button>
    </form>
  );
}
```

En este ejemplo, `submitForm` es una server action que procesa datos del formulario en el servidor, y `ContactForm` es un client component que usa esta action para manejar envíos de formularios.

## Conclusión: Aprovechando Características Modernas para Mejores Aplicaciones Web

React Server Components y Server Actions en Next.js proporcionan herramientas poderosas para construir aplicaciones web eficientes y modernas. Al aprovechar estas características, puedes mejorar el rendimiento, simplificar la lógica del lado del servidor y crear una experiencia de usuario más responsiva.

Mientras construyes tus aplicaciones Next.js, considera incorporar React Server Components y Server Actions para aprovechar al máximo los últimos avances en desarrollo web.

¡Feliz programación!

🚀✨ 